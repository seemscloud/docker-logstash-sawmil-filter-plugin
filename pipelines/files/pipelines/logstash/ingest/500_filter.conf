filter {
    sawmill {
        pipeline => "foo"
        geoip => "GeoLite2-City"
    }

    sawmill {
        pipeline => "bar"
        geoip => "GeoLite2-City"
    }

    sawmill {
        pipeline => "moo"
        geoip => "GeoLite2-City"
    }

    mutate {
        remove_field => [ "@timestamp", "@version", "headers", "host" ]
    }
}