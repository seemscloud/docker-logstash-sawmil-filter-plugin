filter {
#    mutate {
#        remove_field => [ "headers", "@timestamp", "host", "@version" ]
#    }
#    json {
#        source => "message"
#        target => "dupa"
#    }
    sawmill {}
}